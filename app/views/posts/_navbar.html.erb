<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <a class="navbar-brand" href="#">Members Only!</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="<%= posts_path %>">All Posts</a>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= button_to "Sign Out", destroy_user_session_path, :method => :delete, class: "nav-link btn btn-primary" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= button_to "Sign In", user_session_path, class: "nav-link btn btn-primary" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
